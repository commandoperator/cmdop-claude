# v0.1.56 — Add MAX_SCAN_FILES=1000 safety cap

**Date:** 2026-03-10

## Improvements

- `scan_doc_files()` — added `MAX_SCAN_FILES=1000` upper bound after user feedback
- Prevents runaway scanning on monorepos with thousands of markdown files while keeping practical limit high
