# v0.1.58 — Sort marketplace by popularity by default

**Date:** 2026-03-10

## Improvements

- `ClaudePluginsDevSource.search()` — adds `orderBy=stars` param when query is empty
- Default marketplace view now shows most popular skills first (169k+ stars at top)
- Search query still uses relevance ranking from API
