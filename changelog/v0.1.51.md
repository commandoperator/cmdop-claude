# v0.1.51 — Bundled /commit skill

**Date:** 2026-03-10

## Features

- `bundled_skills/commit/SKILL.md` — 8-step release ritual: pre-flight → bump type → `make patch/minor/major` → read version → generate `changelog/vX.Y.Z.md` → stage → HEREDOC commit → optional PyPI publish → push main + version branch → summary
- `SkillService.install_bundled_skills(force=False)` — copies bundled skills to `~/.claude/skills/` using `importlib.resources`
- `Makefile` — `install-skills` target, `setup` now calls `install install-skills dashboard`
- `pyproject.toml` — added `bundled_skills/**/*` to package-data
- `SkillFrontmatter` extended with `license`, `compatibility`, `metadata: dict[str, str]` fields
- `create_skill()` fixed: kebab-case dir names (was using underscores)
- `changelog/` directory created in repo root
