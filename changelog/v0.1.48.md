# v0.1.48 — Parallel embedding with ThreadPoolExecutor

**Date:** 2026-03-10

## Improvements

- `embed_service.py` — parallel batch embedding via `ThreadPoolExecutor(max_workers=8)`, 8x throughput vs sequential
- `EMBED_BATCH_SIZE = 100`, `EMBED_CONCURRENCY = 8`
- `timeout=120.0` added to SDKRouter client to prevent silent hangs on large corpora
- Results preserved in-order via indexed futures
