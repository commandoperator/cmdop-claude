# v0.1.63 — Compact skills list with detail panel and delete

**Date:** 2026-03-10

## Features

- `_installed.py` — two-column layout: compact skill list on left, detail panel on right
- Click skill to open settings and prompt editor without page scroll
- Status indicator: 🟢 auto-invoke enabled, ⚪ manual only
- `delete_skill()` added to `SkillService` — removes skill directory permanently
- Delete button with confirmation dialog in detail panel

## Improvements

- Removed template quick-start section (reduces clutter)
- Filter search bar always visible at top
