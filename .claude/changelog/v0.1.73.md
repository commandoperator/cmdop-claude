# v0.1.73 — Restore MAX_TOTAL_CHARS 400k

**Date:** 2026-03-10

## Improvements
- `review_service.py` — `_MAX_TOTAL_CHARS` restored to 400k (was temporarily reduced to 120k as a workaround for hook timeouts). Now safe since scan runs as background Popen and timeouts no longer affect the hook.
