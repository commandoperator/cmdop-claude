# v0.1.61 — Flexible allowed-tools multiselect

**Date:** 2026-03-10

## Improvements

- `_installed.py` — `st.multiselect` for allowed tools now uses `accept_new_options=True`
- Any tool name can be typed in (including `Bash(git:*)`, custom patterns)
- Options seeded from existing skill tools — no fixed hardcoded list
