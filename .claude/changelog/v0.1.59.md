# v0.1.59 — Fix invalid emoji in st.success

**Date:** 2026-03-10

## Bug Fixes

- `_marketplace.py` — replaced `icon="✓"` with `icon="✅"` in `st.success()`
- Streamlit requires a single emoji character; `✓` (checkmark symbol) is not a valid emoji and caused `StreamlitAPIException` when a skill was already installed
