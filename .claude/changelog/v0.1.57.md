# v0.1.57 — Fix marketplace null version, raise default limit to 100

**Date:** 2026-03-10

## Bug Fixes

- `RegistrySkill.version` changed to `Optional[str]` — API returns `null` for most skills, causing silent `ValidationError` → empty results page
- Default `limit` raised to 100 in `ClaudePluginsDevSource.search()`, `RegistryService.search()`, and `_marketplace.py`

## Improvements

- Marketplace now shows 100 skills per page by default instead of 20
