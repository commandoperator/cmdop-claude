# v0.1.71 — Fix context overflow in review scan

**Date:** 2026-03-10

## Bug Fixes
- `review_service.py` — `_MAX_TOTAL_CHARS` reduced from 400k to 120k (~30k tokens) to prevent `BadRequestError: 400` context overflow on large projects (163k token model limit was being exceeded when file content + metadata + system prompt summed over limit)
