# v0.1.62 — make dashboard auto-reinstalls from source

**Date:** 2026-03-10

## Improvements

- `Makefile` — `dashboard` target now depends on `install`, runs `pip install -e .` first
- Ensures local source changes are always active when running the dashboard
- Removed `PYTHONPATH=./src` workaround (editable install handles it correctly)
