# v0.1.54 — Fix sidecar_scan context overflow

**Date:** 2026-03-10

## Bug Fixes

- `scan_doc_files()` — added `MAX_SCAN_FILES=60`, priority sort: `CLAUDE.md` → `rules/` → other → `plans/`, sorted by mtime desc within group
- `_build_contents_block()` — hard cap `MAX_TOTAL_CHARS=60_000` (~15k tokens), `MAX_PER_FILE_CHARS=3_000`
- Omitted low-priority files noted in output; still visible in metadata list
- Prevents ~613k token overflow that crashed `sidecar_scan` on large `.claude/` directories
